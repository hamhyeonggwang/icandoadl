# 구현 계획 — 3D ADL 훈련과제 저작·실행 도구 v0.1
날짜: 2026-07-12 · 전제: Gate 1 스파이크 셀프 테스트 전 항목 통과 (아동 대상 정식 Gate 1은 별도 진행)
상위 문서: problem-brief_adl-webcam-trainer_v0.1.md — 동결 결정(A1·A1′·A5′·A6·A7)·Out/Exit·C2 전부 계승

## 미션 (2026-07-25 사용자 선언 — 모든 설계의 최상위 기준)
> "이 게임은 단순 옮기기 게임이 아니다. **일상의 재현 및 경험을 최우선**으로 하며
> **자기효능감 향상**을 위한 훈련이다."
주 대상: **발달장애 아동**. 세션 최중요 컨셉: **횡단보도**. → mission-roadmap_v0.1.md

## 목표
치료사가 코드 없이 저작한 Session(JSON)을 아동이 웹캠 PC 브라우저에서 링크로 실행.
러너는 **3D 항행(3인칭) ↔ 거울 모드 조작(스테이션)** 페이즈 교대. 상호작용은 화면 평면 2.5D, 3D는 렌더링 전용(브리프 동결).

## 게임의 인지 목적 (2026-07-12 명문화)
ADL 수행의 신체 훈련을 넘어, 아동이 **상황을 인지하고 실행기능을 경험**하는 것이 핵심 목적:
- **주의·집중**: 게임 흐름을 놓치지 않고 자기 차례·신호를 주시
- **작업기억**: 이전 단계(선택·순서)를 기억해 다음 행동 계획
- **충동 억제**: 즉각 행동 욕구 조절 — 빨간불 기다리기, dwell 유지, 예산 초과 참기
- **결과 예측·인과 추론**: "이 행동을 하면 다음에 무슨 일이?" (신호·예산·순서)
→ 각 과제에 인지기능 배지를 달아 치료사가 목표에 맞게 과제를 선택하도록 함.

## 아키텍처
```
app/
  runner.html / editor.html      ← 빌드 없는 정적 페이지 (퐁퐁 단일파일 철학 계승, 모듈만 분리)
  js/
    params.js        스파이크 PARAMS 계승 (전부 초기 추정 — 캘리브레이션·현장값으로 갱신)
    session.js       Session/Station/Segment 스키마 + 링크 인코딩(base64url) + 데모 세션
    library-meta.js  내장 일상사물 라이브러리 메타 (에디터·러너 공용, three 비의존)
    library-mesh.js  라이브러리 3D 프로시저럴 메시 (three 의존, 러너 전용)
    input-hand.js    HandDriver — MediaPipe Hand+Pose, 캘리브레이션, 분류·스트로크 검출 (스파이크 이식)
    input-mouse.js   MouseDriver — 개발·폴백용 (마우스=손, F/J=교대 스트로크, A/D=기울기)
    runner-app.js    러너 엔진: 페이즈 FSM, 항행 씬, 스테이션 씬, R1/R2 쥐기 FSM, assist
    editor-app.js    에디터: 플로우 편집, 사물 배치, 단계조절, JSON 저장/불러오기, 실행 링크
```
- **입력 드라이버 추상화**: 브리프 Exit 조항("Gate 1 실패 시 마우스·터치 폴백") 선반영. 드라이버 출력 계약:
  `{ hands:{L,R: {present, openness, cls, pos(표시좌표계), vy}}, lean, strokeEvent }`
- **좌표 규약**: 전 계층 표시좌표(거울 반영 후) 정규화 0~1. HandDriver가 `x → 1−x` 변환 담당.
- **C2 불가침**: 영상 프레임 저장·전송 없음. 세션 JSON에도 영상·아동정보 없음.

## 데이터 스키마 (Session v1)
```
Session { version, title, flow: (Segment|Station)[] }
Segment { type:'segment', level:0|1|2, length:'short'|'medium'|'long', theme }
Station { type:'station', title, instruction,
          items:[{lib, pos:[x,y], scale, hand:'any'|'L'|'R'}],   ← 잡기 대상
          target:{lib, pos:[x,y], scale, zoneRadius},            ← 드롭존 (v1: 스테이션당 1개)
          grading:{graspRadius, tReleaseMs, assistTimeoutS} }
```
v1 단순화: 스테이션당 타깃 1개, 모든 아이템 → 동일 타깃 ("가방에 필통·책 넣기"). 다중 타깃 페어링은 v1.1.

## 마일스톤
| M | 내용 | 완료 기준 | 상태 |
|---|---|---|---|
| M0 | 스키마·아키텍처 동결 (본 문서) | 계획 문서 + session.js | ✅ 이 세션 |
| M1 | 입력 드라이버 추상화 | HandDriver(스파이크 이식) + MouseDriver, 동일 계약 | ✅ 이 세션 |
| M2 | 러너 — 스테이션 페이즈(3D 거울 조작) | 잡기/운반/놓기/스냅, R1·R2, assist(타임아웃→반경확대·하이라이트), 추적소실→부유대기 | ✅ 이 세션 |
| M3 | 러너 — 항행 페이즈 + 페이즈 교대 | 교대 스트로크 전진, Level 0/1/2, 체간 조향, 도착→스테이션 전환(카메라 스트림 유지) | ✅ 이 세션 |
| M4 | 에디터 | 팔레트 배치·과제 정의·단계조절 → JSON 저장/불러오기 + 실행 링크 생성 | ✅ 이 세션 |
| M5 | GLB 업로드 + 라이브러리 확충 | 에디터 GLB 임포트(로컬 파일, 업로드 없음), 사물 15종+ | ✅ 2026-07-12 (사물 18종, GLB≤3MB, 러너 '세션 파일 열기' 추가, 드롭존 타원 보정) |
| M5.5 | 마을 콘텐츠 확장 | 횡단보도(crossing 타입) + 장소 시스템 + "우리 마을 하루" 데모 | ✅ 2026-07-12 |
| M5.6 | 치료 깊이 확장 | 세션 리포트(A2 승격) + 변별 과제 + 양손 과제 + 별 보상 | ✅ 2026-07-12 |
| M5.7 | IADL 시뮬레이션 (v1.1 스키마) | 고르기(select) 스테이션 + 예산 장보기 — 버스·키오스크·계산대·길찾기 | ✅ 2026-07-12 |
| M5.8 | 현장 피드백 재정비 | 손들기 폐기·세면대 메시·신발 방향 반전·엘리베이터=버튼 스테이션 | ✅ 2026-07-12 |
| M5.9 | 스테이지 선택 + 인지 목적 명시 | 과제 골라 연습 메뉴, 개별 실행→복귀, 인지기능 배지 | ✅ 2026-07-12 |
| M6.0 | 배포 | GitHub → Vercel → OTHub.kr 링크 | ✅ 라이브 (icandoadl.vercel.app) · 404 수정 완료 · OTHub 링크 대기 |
| M6.1 | Voxel Sandbox 리디자인 | Presentation Layer 전환 (게임 로직·MediaPipe 보존) | ✅ 2026-07-24 |
| M6.2 | 사물 34종 복셀화 | library-mesh.js 전체 BoxGeometry 재구성 | ✅ 2026-07-24 |
| M6.3 | Scene 문법 MVP-A: 침실 | LivingScenePhase — 상태·파생 오브젝트·부분순서 실장 | ✅ 2026-07-24 |
| M6.4 | Scene 문법 MVP-B: 세면실 Diegetic Mirror | 거울=3D 월드 오브젝트, oscillate 신규 | ✅ 2026-07-25 (웹캠 확인 완료) |
| M6.5 | Scene 문법 2편: 마트 심부름 | 목록 암기(작업기억) + zonePass(계산대 스캔) 신규 | ✅ 2026-07-25 |
| M6.6 | 회차 기록 + 훈련 프로파일 (P1) | localStorage 이력·영역 점수·추세·Δ·권고문 | ✅ 2026-07-25 |
| M6.7 | 난이도 사다리 프리셋 (P2) | L1~L5 grading 프리셋·존 링 숨김·레벨 뱃지·에디터 셀렉트 | ✅ 2026-07-25 |
| M6.8 | 적응 제안 (P3) | 완주 후 승인형 레벨 상/하향 제안 + localStorage 영속화 | ✅ 2026-07-25 |
| M6.9 | 걷기 난이도 사다리 (P4) | 게이트·커브·갈림길·미니맵 암기 — NavPhase 조향 판정 | ✅ 2026-07-25 |
| M6.10 | 인지 잔여 차원 (P5) | 예산 계산·정중선 교차 태깅·깃발 이중과제·지연 회상 | ✅ 2026-07-25 |
| M6.11 | 서사 분할 + 횡단보도 승격 (R1·R2) | 1편/2편 세션 분리·TTS·좌우살핌·반복3·공 시나리오 | ✅ 2026-07-25 |
| M6.12 | 시지각 직관성 교정 (R3) | 색 충돌 해소·존 링 신설·핫바 메시 스냅샷·신호등 픽토그램 | ✅ 2026-07-26 |
| M6.13 | 콘텐츠 상세화 (버스·엘리베이터·카페) | 색+번호 버스 노선·2열5행 층버튼·20종 카페 메뉴 | ✅ 2026-07-26 |
| M6 | Gate 2 (저작성 검증) | 미참여 치료사 1명, 무도움 30분 내 스테이션 2개 세션 저작→링크 실행 | 현장 |
| M7 | 러너 검증 | 아동 1명 치료사 동석 세션 완주, 실패 종결 0회 | 현장 |

## M5.5 — 마을 콘텐츠 확장 (2026-07-12 구현·검증)
- **crossing 플로우 타입**: 2차선 도로 + 보행 신호등 + 차량 4대 + 얼룩말무늬 + 건너편 마트 파사드.
  - 훈련 규칙: 빨간불 = 연석에서 전진 잠김(실패 아님, 코칭 문구) → 초록불에만 출발(깜빡임엔 다음 신호 대기) → Level 2는 **손 번쩍 들기(OPEN + 화면 상단)**가 전진 게이트(어깨 거상 훈련 결합) → 건너는 중 신호 변경 시 차량이 정지선 대기(실패호 제거).
  - 신호 음향(초록 멜로디·깜빡임 틱) = 실제 음향신호기 유사, 시각+청각 단서.
  - 단계조절: Level 0/1/2, 빨간불·초록불 길이 조절.
- **장소(place) 시스템**: 스테이션에 마을 공간 11종(아이방·주방·화장실·세면대·현관/신발장·문구점·마트·카페·식당·학교) — 장소 칩 + 색 데코 + (마우스 모드) 배경 틴트.
- **항행 테마 6종**: 집 복도·엘리베이터·길거리·공원·마트 통로·학교 복도.
- **라이브러리 34종**: 과일(바나나·귤·포도·딸기), 빵·주스, 신발·옷, 연필·공책, 드롭존(장바구니·신발장·옷장·세면대·접시·책상) 등 추가.
- **데모 세션 "우리 마을 하루"**: 아이방(옷 정리)→세면대(양치)→주방(아침)→현관(신발)→엘리베이터→길거리→🚦횡단보도→마트(과일 담기)→문구점→카페→학교 복도→교실(책상 정리) — 마을 한 블록 완성.
- 도로는 crossing이, 엘리베이터는 항행 테마가 담당 (스테이션은 조작 과제가 있는 공간만).

## M5.6 — 치료 깊이 확장 (2026-07-12 구현·검증)
- **세션 리포트 (A2 승격 결정)**: 자동 기록은 치료사 동석 관찰의 *보조*로 승격. C2 준수 — 파생 수치만, 온디바이스, 완료 화면 요약 + JSON/CSV 로컬 다운로드. 지표: 스테이션(잡기 시도/빈손, 존 밖 놓기, 방해자극 시도, 추적소실, 도움 단계, 소요시간), 횡단보도(신호대기·건너기 시간, 대기 중 출발시도, 손내림 횟수), 이동(스트로크 수).
- **변별 과제(distractor)**: "과일**만** 담아요" — 방해 자극을 존에 넣으면 부드럽게 되돌아옴(실패 아님, 지표만 기록). assist 부유 이동에서 제외. 에디터 체크박스 + 🙅 뱃지.
- **양손 과제(hand:'both')**: 큰 사물은 두 주먹이 모두 반경 안이어야 들림, 두 손 중점을 따라 이동, 한 손 놓기/추적소실 시 전체 해제. **R1은 양손 과제에 한해 완화**(양손 동시 조건이 이미 상위 난도) — 결정 근거 기록. 브리프 목표 '양측 상호교대 협응'을 조작 과제로 직결.
- **별 보상**: assist 단계를 척도로 재활용 — 무도움 ⭐⭐⭐ / 1단계 ⭐⭐ / 2단계 ⭐. 완료 배너·리포트·세션 총계 표시. 완료 후 assist 동결(별-도움 불일치 버그 수정).
- **다음 후보 (보류)**: 횡단보도 좌우 살피기(고개 회전) — Pose yaw 신뢰성 미검증, A8류 스파이크 후 결정.

## M5.7 — IADL 시뮬레이션 (2026-07-12 구현·검증)
**아키텍처 결정 (v1.1)**: 버스 노선 확인·키오스크 주문·셀프 계산대·길찾기 앱의 공통 원형은
"화면 앞에서 고르기". 페이즈 폭발 대신 스테이션 `kind: 'move'|'select'` 하나로 통합 (단독 유지보수 제약).
- **고르기(select) 스테이션**: 선택 카드 패널(캔버스 텍스처 — 외부 자산 없음, C2 유지). 순차 스텝 스크립트
  (예: 키오스크 = 메뉴 선택 → 결제 버튼). 선택 동작 = **OPEN 손 dwell(머무르기)** — 상지 도달+유지 훈련.
  오답 = 흔들림+기록(실패 없음). assist: 타임아웃 → 정답 강조 → 오답 흐리게. 단계조절: dwellMs 400~2000.
- **예산 장보기(셀프 계산대)**: move 스테이션 옵션 — `budget`(원) + 사물 `price`(가격표 스프라이트) +
  `requiredCount`(N개만 골라 담기). 예산 초과 담기 = 되돌아옴(실패 없음, 지표 기록).
  **담은 것 빼기 허용** — 검증 중 발견한 조합 데드락(비싼 것 먼저 담으면 완료 불가)의 해법이자
  실제 계산대의 '빼기' = 조합 재선택(집행기능) 훈련.
- **데모 14항목**: 버스 정류장(3번 버스 고르기→앞문 타기), 마트 예산 장보기(5,000원·2가지),
  카페 키오스크(딸기주스→결제), 길찾기 앱(방향 고르기) 추가. 장소 busstop·street 추가.
- 리포트 지표 추가: 오선택(wrongSelects)·예산초과 시도(budgetOvers)·스텝 수.
- 에디터: "+ 고르기" 버튼, 스텝/선택지 편집기(정답 체크), dwell 슬라이더, 예산·가격 필드, 카드 미리보기.

## M5.8 — 현장(웹캠) 피드백 재정비 (2026-07-12)
1. **횡단보도 손들기 폐기** — 원인 규명: 스트로크(양손 주먹)와 손들기(편 손 위로)가 **동작 모순**이라
   Level 2에서 이동 불가. 게이트 삭제, Level 0 자동/1 스트로크로 단순화. "상황에 맞는 이동" 초점.
   합성 입력 검증: 빨간불 출발시도 17회 차단 → 초록불 스트로크 22회로 완주.
2. **세면대 메시 재설계** — 수전이 z축 뒤에 있어 정사영 뷰에서 분리돼 보이던 문제 →
   받침대-세면볼-수전기둥-스파우트 일체형(같은 평면).
3. **신발 과제 방향 반전** — 외출 서사에서 신발은 신발장에 '넣는' 게 아니라 '꺼내 신는' 것 →
   신발 → 발판(footmat 👣, 신규 사물) 과제로 교체.
4. **엘리베이터 = 버튼 스테이션** — 전진 항행(실외처럼 보임) 폐기 → "1층 버튼을 눌러요" 고르기
   스테이션(장소 🛗 신규). 엘리베이터 항행 테마는 에디터에 잔존(선택 가능).
5. **데모 v3 "우리 마을 하루" (15항목)** — 일상 서사 순서로 재배열: 옷 갈아입기 → 세면대 → 아침 →
   신발 꺼내 신기 → 🛗 1층 버튼 → 골목 걷기 → 🚏 버스 → 큰길 → 🚦 횡단보도(L1) → 🛒 예산 장보기 →
   ✏️ 문구점 → ☕ 키오스크 → 🛣️ 길찾기 → 학교 복도 → 교실(양손 책).
- 난이도 호: 단순 옮기기(아침) → 선택 1스텝 → 신호 준수 → 예산·변별 → 다단계 선택 → 양손 협응(종착).

## M5.9 — 스테이지 선택 + 인지 목적 명시 (2026-07-12 구현·검증)
- **두 진입점**: 시작 화면에 "🌟 전체 하루 시작"(순차) / "🎯 과제 골라 연습"(선택).
- **과제 선택 메뉴**: 세션 flow 각 항목을 카드로 나열 — 번호·이모지·제목·유형(옮기기/걷기/횡단보도/고르기)
  + **인지기능 배지**(주의·작업기억·충동억제·인과추론 등, 실행기능 태그 우선 slice(0,3)).
- **개별 실행 → 메뉴 복귀**: 한 과제를 끝내면 별점 토스트와 함께 메뉴로 돌아옴(연습한 카드는 dim+별점).
  여러 과제를 필요에 따라 골라 반복 연습 가능 — 전체 하루를 다 할 필요 없음.
- **캘리브레이션 분리**: 웹캠 모드에서 "🖐️ 준비운동" 카드로 독립. 다른 과제 첫 선택 시 미완료면 자동 선행,
  이후 과제는 스킵. 마우스 모드는 카드 숨김(불필요). `ensureDriver()`/`runCalibration()`로 리팩터링.
- 검증: 마우스 모드에서 메뉴→개별 실행(횡단보도·물건옮기기)→복귀→별점, 뒤로→시작, 전체 하루 순차 실행 확인.

## M6.0 — 배포 (진행 중)
- **GitHub 푸시 완료** (2026-07-12): `https://github.com/hamhyeonggwang/icandoadl` main 브랜치.
  독립 git 저장소로 init(상위 Project repo와 분리). settings.local.json 자동 제외, files.zip/DS_Store gitignore.
- 배포 구조: 루트 `index.html` → `./app/` 리다이렉트, `app/index.html` = 랜딩(러너/에디터).
  리라이트 대신 리다이렉트 방식 — 상대경로 ES module import가 깨지지 않도록.
- **Vercel 배포 완료** (2026-07-12): `https://icandoadl.vercel.app`. 저장소 루트 서빙(파일은 `/app/` 아래).
- **404 버그 수정**: 초기 `vercel.json`의 `cleanUrls`+`trailingSlash:false`가 랜딩 `/app/`→`/app`으로 만들어
  상대링크 `./runner.html`을 `/runner`(404)로 깨뜨림. → vercel.json 제거(순수 정적 기본 동작),
  루트 리다이렉트를 `./app/index.html` 명시 경로로 변경. 라이브 검증: `/`→랜딩→`/app/runner.html` 정상 로드.
- **다음(사용자)**: 배포 URL(`https://icandoadl.vercel.app/`)을 OTHub.kr에 링크.
  선택: Vercel Root Directory=`app`으로 설정 시 `/app/` 접두어 없이 `/runner.html`로 깔끔해짐.

## 실패호 제거 설계 (러너)
- 놓기 실패(존 밖) → 사물이 그 자리에 남음, 재쥐기 가능. 실패 이벤트 없음.
- 스테이션 타임아웃 → assist 1단계: 잡기반경·존 반경 ×1.6 + 타깃 펄스 하이라이트. 2단계: 아이템이 타깃 방향으로 천천히 부유.
- 추적 소실(CARRY 중 손 미검출 0.5s) → 사물 제자리 부유 대기, 손 재검출 시 재개.

## Out 유지 (브리프 계승)
물리엔진 없음(스냅·드롭존 판정) · WebXR/모바일 없음 · 수행기록 v1 제외 · 인벤토리·분기 없음 · 클라우드 없음.

## M6.1 — Voxel Sandbox 리디자인 (2026-07-24 구현·검증)
**원칙**: Minecraft의 디자인 "원리"만 차용(블록·픽셀·슬롯·하드섀도·HUD 최소화) — 텍스처·리소스 비복제.
MediaPipe 파이프라인·게임 로직·판정 알고리즘 **무변경** (Presentation Layer만 교체).
- **디자인 시스템** `app/styles/voxel-ui.css`: Voxel World Palette(독자 값) + vx-btn/panel/slot/progress/chip
  토큰. Display=DungGeunMo(한글 픽셀 폰트, CDN) / Body=고가독성 산세리프.
- **화면**: 랜딩·타이틀·과제 메뉴·HUD·리포트·캘리브 전부 직각+하드섀도+눌림 피드백. 스캔라인 배경.
- **게임 오브젝트**: 손 커서=복셀 브래킷 레티클(`.material` 인터페이스 유지), 아바타=블록형(팔 계약 유지),
  나무=복셀 트리, 선택 카드·가격표=픽셀 캔버스 텍스처. 스테이션 사물 34종 복셀화는 Phase 4 백로그.
- **핫바(기능 재해석)**: 스테이션=목표 사물 슬롯(완료 ✔), 고르기=스텝 슬롯(정답 이모지는 완료 후만 공개).
- **신규**: 복셀 로딩 화면(실제 초기화 단계 표시 — HandDriver.start(onStage) 콜백), 손 미검출 배너
  ("손이 안 보여요" — 거울 과제·웹캠 모드), 음소거 토글(M키), 시작 화면 카메라 안내문.
- **버그 수정**: stageAspect() 0-크기 순간 NaN → 16:9 폴백 (NaN 지오메트리 근본 차단).
- 검증: 마우스 모드 E2E(타이틀→메뉴→스테이션→걷기→고르기), FPS 유지, 씬 NaN 0.
- 에디터는 치료사용 대시보드로 의도적 미적용(게임 아님) — 추후 결정.

## M6.2 — 사물 34종 복셀화 (2026-07-24 구현·검증)
`library-mesh.js`의 구·원통·원뿔·토러스·캡슐 지오메트리를 전부 BoxGeometry 조합으로 재작성.
곡선(바나나 곡률·딸기 원뿔)은 각도 차를 준 박스 세그먼트로 근사, 원형 파츠(장바구니 손잡이 링·
가위 손잡이·컵꽂이)는 `ringFrame()`(얇은 박스 4개 정사각 프레임) 헬퍼로 대체. `mat()` 기본값에
`flatShading:true` 추가로 전 사물이 각진 셰이딩 통일. 판정·스케일(meta.size) 로직 무변경.
- **버그 수정**: 카트 손잡이 기둥이 바구니와 떠 보이던 문제 → 기둥 바닥 y를 바구니 top(0.6)에
  정확히 맞춤. 바퀴 색을 배경과 구분되는 회색(#4a5568)으로 변경.
- 검증: 새 탭(콘솔 이력 오염 없음)에서 34종 중 아이방·주방·세면대·마트(과일)·현관(신발/발판)·
  교실(양손 책+책상) 스테이션을 순회 스크린샷 확인, 콘솔 에러 0.
- **콘솔 NaN 로그는 오탐**: three.js를 쓰지 않는 index.html에서도 동일 에러가 나타나
  이 브라우저 도구의 콘솔 버퍼가 탭 재사용 시 지워지지 않고 누적됨을 확인(실제 버그 아님).
  이후 검증은 항상 새 탭(tabs_create)에서 진행할 것.

## 검증 계획 (이 세션)
MouseDriver로 브라우저 셀프 테스트: 에디터 저작→링크 생성→러너 실행→항행→스테이션 완료 E2E.
HandDriver 경로는 사용자가 웹캠 PC에서 Gate 1 체크리스트 재사용 검증.

## M6.3 — Scene 문법 MVP-A: 침실 (2026-07-24 구현·검증)
scene-grammar_v0.1.md §MVP-A를 실제 구현. Migration Map F1대로 데모 세션 flow[0]을
"일어나서 옷 갈아입기"(station)에서 `makeBedroomMorningScene()`(livingScene)로 교체.

**신규**: `session.js`에 `makeLivingScene()`/`makeBedroomMorningScene()`(envObjects·props·
onComplete·exit 데이터), `runner-app.js`에 `LivingScenePhase` 클래스. 판정은 StationPhase의
GRASP/CARRY/RELEASE FSM·양손 잡기를 그대로 재사용(신규 없음), interaction 신규 2종만 추가:
- **slide**(커튼): 축 제약 드래그, 임계 거리 도달 시 완료·자동 해제
- **bimanualLift**(이불): 양손 중점의 축 이동량이 임계값 도달 시 완료, 접힌 위치로 스냅

**파생 오브젝트(P8) 구현**: `pajama` prop은 `startHidden:true`로 시작(리스트엔 존재, mesh·판정
없음) → `clothes`(옷 입기) 완료 시 `onComplete.reveal`로 최초 노출·mesh 생성. 이후 `pajama`를
빨래바구니 존으로 옮기면 최종 완료(exit) — "행동의 결과가 다음 행동의 이유를 만든다"(P5)를
그대로 구현.

**환경(비상호작용)**: window·bed·closet(기존 재사용)·laundryBasket — envObjects로 배경 렌더.

**신규 라이브러리**: bed·window·curtain·laundryBasket·blanket(복셀 박스 조합) + pajama(shirt
빌더 재사용, 색만 분리). `shirt` 빌더를 `buildShirt()` 함수로 추출해 `pajama`와 공유.

**에디터 방어**: `f.type==='livingScene'`일 때 "편집 준비 중" 플레이스홀더로 조기 반환(크래시
없음, `validateSession`도 livingScene을 허용 타입에 추가).

**검증 (마우스 모드, 합성 입력 정밀 제어)**:
- 커튼 슬라이드: 잡기→축 방향 드래그→임계 도달→`done`, 손 자동 ARMED 복귀 ✓
- 이불 양손: 두 주먹 동시 잡기→중점 상승→임계 도달→접힌 위치 스냅 ✓
  - **버그 발견·수정**: 완료 직후 두 손을 `ARMED`로 보내면 forEach 처리 순서상 아직
    처리 전인 손이 "빈 주먹"으로 오판정돼 즉시 LOCKED+graspFail 오발생(R1 오작동) →
    완료 시 두 손 다 `LOCKED`로 보내도록 수정, 대칭·graspFails=0 확인
- 옷 갈아입기→잠옷 파생→빨래바구니: 전체 체인 완주, `outfit=dayclothes`, exit 배너
  "⭐ 이제 세수하러 가요" 정상 표시 ✓ (t_release 타이머는 실제 경과시간 기반이라 합성
  루프 테스트 시 `openHoldStart`를 직접 되돌려 검증 — 테스트 기법 메모)
- "전체 하루" 15항목 전체 완주(⭐×31), 리포트 1행에 침실 Scene이 기존 `entrySummary()`
  포맷 그대로 표시(특수 분기 불요) — 콘솔 에러 0
- 에디터: 새 세션 로드 시 크래시 없음, 플레이스홀더 정상 표시, 팔레트 비활성화 정상

## M6.4 — Scene 문법 MVP-B: 세면실 Diegetic Mirror (2026-07-25 구현·검증)
dual-perspective-system_v0.1.md §5·§6을 실제 구현. §6 리스크 5개를 착수 전 확정:
1. **종횡비**: object-fit:cover 방식 UV crop(`vt.repeat`/`vt.offset`)으로 카메라(16:9)와 거울
   화면비 불일치 해결 — `videoAspect` vs `mirrorScreenAspect`(월드 크기를 stageAspect()로
   화면비 환산) 비교 후 넘치는 축만 크롭.
2. **손 좌표 재투영**: §3 스코프 결정대로 별도 좌표계 도입 없이 기존 전체 화면 `toWorld()`
   매핑을 그대로 사용 — 거울은 화면의 큰 부분을 차지하는 시각 오브젝트일 뿐, 판정은 무관.
   손이 거울 밖으로 나가면 커서가 벽(voxel 배경) 위에 보이는 것을 의도된 단순화로 수용.
3. **전환 연출**: 문서 권고대로 기존 페이드 컷 유지, 카메라 다가가기는 보류.
4. **성능**: 이 샌드박스는 웹캠이 차단돼 있어 실측 불가 — 사용자 웹캠 테스트 시 FPS 배지로
   직접 확인 필요(정직하게 미검증 상태로 남김).
5. **재사용성**: `scene.mirror`(위치/크기)·`scene.roomBackdrop`(배경색)을 `LivingScenePhase`의
   범용 필드로 구현 — 향후 다른 거울 있는 장소(현관 전신거울 등)도 필드만 채우면 재사용.

**신규 recognition은 oscillate(왕복) 1개뿐** — 나머지는 기존 GRASP/CARRY/RELEASE 재사용(C절
약속 그대로). 손에 쥔 사물의 프레임 간 x변위로 방향 반전을 세는 방식으로, 걷기의 교대 스트로크
피크 검출과 동일 원리를 재사용해 구현(신규 드라이버·MediaPipe 변경 없음).

**세면실 장면**: `makeWashstandMirrorScene()` — 칫솔 왕복(oscillate, 필수) 1개 행동.
`videoWrap`(풀스크린 DOM 웹캠)을 숨기고, 대신 `stScene` 안 거울 평면(`THREE.VideoTexture` →
`driver.videoEl`)에 실시간 영상을 매핑. 마우스 모드(videoEl 없음)는 자리표시 색면으로 안전
대체. 거울 반전은 CSS가 아니라 `mesh.scale.x=-1`(지오메트리 반전)로 처리 — VideoTexture는
DOM CSS transform을 무시하고 원본 프레임을 읽으므로 별도 반전이 필요.

데모 세션 flow[1]을 기존 "세수와 양치"(station, 칫솔·비누를 세면대로 나르는 발명된 행동)에서
이 Scene으로 교체(생태학적 타당성 분석의 "무의미한 운반" 지적을 해소).

**검증(마우스 모드)**: 거울 진입 시 `videoWrap` 자동 숨김·거울 메시 생성 확인, 칫솔 잡기→좌우
왕복 8회(진폭 0.08)→`done`+`hygiene=teethBrushed`+완료 배너 확인, "전체 하루" 15항목 재완주
(⭐×33) 회귀 없음, 에디터 플레이스홀더 정상. 콘솔 에러 0.

**웹캠 실기 확인 완료(2026-07-25, 사용자)**: 거울 텍스처·왕복 판정 정상. 별도 성능 이슈 없음.
OSC_MIN_VX(0.4)·oscTarget(8)은 여전히 초기 추정 — 필요시 현장 감각으로 미세 조정 가능.

## M6.5 — Scene 문법 2편: 마트 심부름 (2026-07-25 구현·검증)
scene-grammar_v0.1.md §8의 "2편 심부름 다녀오기" 중 마트 파트를 구현. **신규 recognition은
zonePass(경유 스캔) 1개** — 이걸로 C절이 예고한 신규 판정 2종(oscillate·zonePass)이 모두 실장됨.

**핵심 설계 결정**: 예산(가격) 방식 대신 **심부름 목록 암기**를 채택. 이유는 ecological-validity
분석의 지적("예산이라는 규칙"보다 "엄마 심부름이라는 생활 이유"가 방해자극과 결합할 때 더
자연스러움) — `scene.purpose`에 목록("엄마가 사과랑 우유를 사 오라고 하셨어요!")을 표시했다가
`listRevealS`(6초) 후 `genericPrompt`로 전환해 화면에서 사라지게 함으로써 진짜 회상(recall)을
유발. 막히면 기존 assist 1단계가 미완료 필수 사물의 occupation을 개별 힌트로 보여줌(재사용).

**ZONE_PASS(신규)**: carryToZone 운반 중 `prop.scanZone`을 지나면 `_scanned=true`(삑 소리+
파티클), 카트에 놓을 때 `_scanned`가 없으면 완료 대신 반려("계산대를 먼저 지나가야 해요") —
"놓기"가 아니라 "통과"로 판정한다는 점에서 기존 carryToZone/slide와 구별되는 신규 원리.

**방해자극(사탕)**: 목록에 없는 유혹 — 카트에 넣어도(스캔 여부 무관) 반려, `wrongItem` 지표에
기록. StationPhase의 기존 distractor 개념을 LivingScenePhase의 `completeCarryToZone`에도
이식(`bounceProp()` 공용 헬퍼로 정리 — 존 밖 미스·방해자극·스캔누락 3가지 반려를 공유).

**신규 라이브러리**: milk·candy·shelf·checkoutScanner(전부 복셀 박스 조합).

**교체**: 데모 flow의 기존 "마트 예산 장보기"(station, 5000원/2종 규칙)를 이 Scene으로 전면
교체 — Migration Map(ecological-validity §F, 항목 10) 그대로 실행.

**검증(마우스 모드)**: 자연 경로(직선)로 옮기면 우연히 스캐너를 지나 자동 스캔되는 것 확인(현실
동선과 자연스럽게 겹침) → 의도적 '점프'로 스캔 없이 카트 접근 시 정확히 반려됨을 별도 검증 →
스캐너 통과 후 정상 담기 → 사탕은 스캔 통과해도 반려(`wrongItem++`) → 사과+우유 완료 시 exit
배너 "결제 완료! 이제 집에 가요" 정상. "전체 하루" 15항목 재완주(⭐×33) 회귀 없음, 리포트에
마트 행이 기존 포맷 그대로 표시, 에디터 플레이스홀더 정상. 콘솔 에러 0.

**다음(미착수)**: 문구점 결제·카페 간식을 2편 오후 흐름으로 통합할지 여부, 예산 개념을 이
Scene에 추가 레이어로 결합할지(현재는 스코프에서 제외).

## M6.6 — 회차 기록 + 훈련 프로파일 (P1) (2026-07-25 구현·검증)
difficulty-progression_v0.1.md의 우선순위 P1 실장 — Q3("결과가 시각적이고 다음 훈련 지표가
되는가")를 해소.

**회차 기록**: `adl-history-v1`(localStorage, 최근 30회) — `{at, title, stars, domains}`.
C2 준수: 파생 수치·별점만, 아동 식별정보 없음, 기기 로컬 한정. 기록 대상 = 완료 화면 도달
회차(전체 하루 완주). 개별 연습은 기존대로 별점 토스트만(기록 안 함 — 회차 정의의 일관성).

**영역 점수 4종** (0~100, 공식 투명·코드 주석에 명기, 데이터 없으면 "자료없음"):
- 조작 정확도 = mean(잡기 성공률, 놓기 정확률)
- 선택·주의 = 정답 선택 / (정답+오선택+방해자극 시도)
- 충동 억제 = 100 − 20×(빨간불 출발 시도) [횡단보도 포함 회차만]
- 자립도 = 1 − Σassist/(2×과제 수)

**완료 화면 개편**: ①영역 프로파일 바 4개(+지난 회차 대비 ▲/▼/= Δ), ②회차별 별점
스파크라인(canvas 직접 그리기, 복셀풍 사각 점, 최근 15회, 현재 회차 하이라이트),
③규칙 기반 다음 훈련 권고문(최대 3개 — "임상 판단 대체 아님" 원칙, 조건·문구는
difficulty-progression 문서 그대로). 신규 버튼: "기록 CSV"(회차별 별점·영역 점수 내보내기),
"기록 지우기"(확인 후 삭제 — 지우기 전 내보내기 안내).

**검증(마우스 모드, 새 탭)**: 1회차 완주 → 프로파일(92/86/100/100)·스파크라인 1점·권고문
("도움 없이 안정 수행 — 난이도 상향 제안") 발동 확인 → 2회차 완주 → Δ 열 "=" 표시·추세선
2점(마지막 노란 하이라이트)·history 2건 저장 확인. 기록 CSV 클릭 무오류. 콘솔 에러 0.
report JSON에 runNo 포함.

**다음(미착수)**: P2 레벨 사다리 grading 프리셋, P3 적응 제안 카드(P1 데이터 기반),
⚠️ 공용 PC 아동 분리(프로필) 방식은 치료사 결정 대기.

## M6.7 — 난이도 사다리 프리셋 (P2) (2026-07-25 구현·검증)
difficulty-progression_v0.1.md §A·§D-P2 실장 — "반복 횟수가 아닌 난이도 차원"의 1차 구현.

**DIFFICULTY_PRESETS (session.js)**: 과제 유형별 L1~L5 grading 묶음(수치 전부 초기 추정).
- move 계열: graspRadius 0.10→0.04 · tReleaseMs 200→450 · **zoneScale 1.3→0.6**(존 판정 배율,
  신규) · **zoneRingVisible**(L5=존 링 숨김 — 위치 회상 요구, 단 assist 1 발동 시 재표시 = 도움의
  본질 유지) · assistTimeoutS 15→30.
- select 계열: dwellMs 600→1500(유지 지속 요구) · assistTimeoutS.
- memoryRevealS: 목록 노출 L1=상시(0) → L5=3초 (livingScene의 listRevealS에 매핑).

**적용 방식(설계 결정)**: 프리셋은 **저작 시점**에 item.grading에 기입(applyDifficultyPreset) —
러너는 grading만 읽는 단순성 유지, 에디터 슬라이더 미세조정은 프리셋 이후 값이 그대로 우선.
make 헬퍼들이 difficulty 전달 시 자동 적용.

**러너 신규 반영**: StationPhase·LivingScenePhase 존 판정에 zoneScale 곱, StationPhase 존 링
가시성(숨김+assist 재표시), 스테이지 카드에 "Lv.N" 뱃지, 리포트 entries·CSV에 lv 열.

**에디터**: 옮기기·고르기 속성 패널에 "난이도 프리셋" 셀렉트(L1~L5/직접 설정) — 선택 시 grading
기입·슬라이더 갱신·플로우 카드 라벨에 "· LN". 직접 설정 선택 시 difficulty 제거(미세조정 모드).

**데모 난이도 배정**: 아침 식사 L1 → 신발·문구점·버스·키오스크 L2 → 마트·교실 L3. 침실·세면실
Scene L2, 마트 Scene L3(listRevealS=6 유지). crossing·segment는 기존 자체 level 유지(걷기
사다리는 P4). 길찾기는 미지정(삭제 예정 항목).

**검증(마우스 모드, 새 탭)**: Node로 데모 15항목 프리셋 적용값 전수 확인 → 메뉴 카드 뱃지 15개
정확 → L1 런타임(0.10/1.3/200ms) 확인 → L5 강제 주입: 존 링 숨김 + assist 1 발동 시 재표시
확인 → 에디터 L1→L4 변경 시 grading·슬라이더·카드 라벨 갱신 확인 → 전체 하루 회귀(⭐×33,
zoneScale 0.8 마트 포함) 통과, 콘솔 에러 0.

**다음(미착수)**: P3 적응 제안 카드(회차 기록 기반 "한 단계 어렵게?" — 치료사 승인형),
P4 걷기 사다리(게이트·커브·갈림길·미니맵), P5 이중과제·계산. 사다리의 신체 배치 차원
(정중선 교차·상단 대역)은 좌표 프리셋이 필요해 P2에서 제외 — P5와 함께 검토.

## M6.8 — 적응 제안 (P3) (2026-07-25 구현·검증)
difficulty-progression_v0.1.md §B·§D-P3 실장. P1(회차 기록)·P2(레벨 사다리)가 있어야 가능한
마지막 조각 — "1→15에서 성장하는가"(Q2)에 처음으로 "예, 단 항상 사용자 승인 하에"로 답한다.

**규칙(투명·자동 아님)**: 완주 직후 각 과제 지표를 보고 —
- 도움 0 + 핵심 지표 충족(옮기기: 빈손≤1·존밖≤1·방해자극 없음 / 고르기: 오선택 0) → **상향 제안**
- 도움 2 발동 → **하향 제안**(L1에서는 더 내려가지 않음, L5에서는 더 올라가지 않음)
제안은 `adl-suggest-v1`(localStorage)에 저장되어 **다음 시작 화면**에 카드로 뜬다("🌱 성장
제안" — 과제별 L{from}→L{to} + 이유, 최대 4개 표시 + "외 N개"). **자동 적용되지 않는다** —
"좋아요, 바뀌요"를 눌러야 `adl-level-overrides-v1`에 저장되고 해당 회차부터 반영된다.
"이번엔 그대로"는 제안만 지우고 레벨은 그대로 둔다.

**영속화**: 오버라이드는 과제 **제목**을 키로 저장 — 세션 로드(loadSession 직후, 세션파일 열기
경로 포함)마다 `applyLevelOverrides()`가 해당 과제의 difficulty를 덮어쓰고
`applyDifficultyPreset()`을 재적용한다. 새로고침·재방문에도 유지(성장이 사라지지 않음).

**"기록 지우기"에 통합**: 회차 기록·대기 제안·레벨 오버라이드 3종을 한 번에 초기화(확인+내보내기
안내 유지) — 셋을 따로 지우면 상태 불일치가 생기므로 하나의 리셋 지점으로 묶었다.

**검증(마우스 모드, 새 탭)**:
- 규칙 합성 검증: 하향(도움2)·상향 차단(방해자극 있음)·상향(고르기 오선택0)·상한(L5 이상 제안
  없음) 4케이스 정확.
- 전체 하루 완주 → 제안 10개 자동 생성·카드 표시 확인 → "좋아요" 클릭 → 오버라이드 저장·현재
  세션 즉시 반영(마트 graspRadius 0.055→0.045 확인)·안내문 갱신.
- **새로고침 후 오버라이드 유지** 확인(핵심 — 성장의 영속화).
- "이번엧 그대로" 별도 검증: 오버라이드 불변, 제안만 삭제.
- "기록 지우기" → 3키 모두 삭제 → 새로고침 시 기본 레벨(L1~L3)로 정확히 복귀.
- **상향된 레벨(L3~L4)로 전체 하루 재완주** — 회귀 없음(⭐×33), 완주 후 다음 제안 10개 재생성
  확인(성장 루프 지속성). 콘솔 에러 0.

**다음(미착수)**: P4 걷기 사다리(게이트→커브→갈림길→미니맵, 최대 규모), P5 이중과제·계산·
신체 배치 차원(정중선 교차 등). ⚠️ 공용 PC 아동 분리(프로필)는 여전히 치료사 결정 대기 —
오버라이드·기록이 기기 전역이라 여러 아동이 한 기기를 쓰면 레벨이 섞인다.

## M6.9 — 걷기 난이도 사다리 (P4) (2026-07-25 구현·검증)
difficulty-progression_v0.1.md §A "걷기" 실장. Q1 감사에서 "걷기 = 단순 직선거리"였던 부분을
채운다 — 이제 걷기도 조향 정확도·지속 조향·판단력·공간기억까지 사다리로 성장한다.

**스키마(session.js)**: `makeSegment`에 `courseType`('straight'|'gate'|'curve'|'fork'|'forkMemory'),
`gates`(구간비율 z + xOffset + width), `curveAmp`/`curveFreq`(중심선 사인파), `forks`(구간비율 z +
correct 'L'|'R'), `mapRevealS`(출발 전 경로 노출 초) 필드 추가. `COURSE_LEVELS` L1~L5 프리셋 테이블
+ `applyCourseLevel(segment, lv)` 저작 시점 적용 헬퍼 — L1 직선 / L2 게이트 1쌍 / L3 커브 / L4
갈림길 1회 / L5 갈림길 2회+미니맵 4초.

**의도된 단순화(투명하게 기록)**: L4·L5의 갈림길은 실제 분기 지오메트리(경로가 갈라졌다 다시
만나는 구조)가 아니라 **판정 지점 방식** — 코스는 여전히 단일 직선/커브이고, 지정된 z 지점을
지날 때 그 순간의 좌우 위치(x 부호)만으로 방향 선택을 판정한다. 충돌·경로 재수렴 로직 없이
안전하게 구현하기 위한 의도된 스코프 축소이며, 목표로 하는 인지 과제(표지판 읽기·즉각 판단·
공간기억 회상)는 그대로 유지된다. 마트 심부름(M6.5)의 예산 vs 목록 결정, 세면실(M6.4)의 거울
전용 Diegetic Mirror 스코핑과 같은 맥락의 투명한 범위 결정.

**runner-app.js**: `buildNavCourse()`에 게이트 기둥(주황 복셀 포스트 2개)·갈림길 표지판
(`makeNavSign` — fork는 방향 화살표 공개, forkMemory는 '❓'로 정답 비공개) 배치 추가, 커브
코스는 `laneCenterX(segment, z)` 중심선 함수로 차선 마커·좌우 프롭·도착 게이트까지 전부 사인파를
따라 휘어짐. `NavPhase`에 `steerCourse` 플래그(게이트/커브/포크 계열은 `level` 값과 무관하게
조향 활성화), 게이트/갈림길 1회성 판정(`gateState`/`forkState`, **비처벌** — 실패해도 진행은
계속), 커브 이탈량 관찰 지표(`laneDeviationAvg`, 감점 아님), 미니맵 암기 타이머(`mapRevealS` —
마트 `listRevealS`와 동일 패턴, 노출 중엔 이동 정지). 신규 지표
`gatesPassed/gatesTotal`·`forksCorrect/forksTotal`·`laneDeviationAvg`는 기존
`Object.assign(e, phase.metrics)` 리포트 파이프라인에 자동으로 실림. 게이트/갈림길이 있는
구간만 별점 부여(전부 통과 3⭐·일부 2⭐·완주만 1⭐) — 기존 순수 직선 구간은 여전히 무별점 유지
(회귀 없음). `stageLabel`/`cognitiveTags`/`entrySummary`/`REPORT_COLS`도 courseType별 배지
("조향정확도"/"지속적조향"/"방향판단"/"공간기억")와 게이트·갈림길 수치를 반영하도록 확장.

**데모 세션 배치**: 등교 3구간 중 첫 구간(버스 정류장 전)=게이트 L2, 둘째 구간(횡단보도
전)=커브 L3, 셋째 구간(교실 도착 전, 길찾기 앱 직후)=갈림길 2회+미니맵 L5 — "길찾기 앱"에서
방향을 고른 직후 실제로 그 방향 감각을 몸으로 써먹는 서사적 흐름으로 배치.

**검증(마우스 모드, 새 탭)**:
- 과제 골라 연습에서 게이트(L2)·커브(L3)·갈림길+미니맵(L5) 3종 개별 실행 — 각각 안내문·
  조향 활성화·표지판 표시 정확.
- 게이트: 중앙 유지 시 2/2 통과·⭐⭐⭐, 후반 이탈 유도 시 1/2 통과·⭐⭐(비처벌로 완주 자체는
  정상 진행) 둘 다 확인.
- 커브: 목표 중심선 추종 시 `laneDeviationAvg` 낮게 기록됨 확인.
- 갈림길 암기(L5): 출발 전 "🗺️ 경로를 기억하세요: ➡️ → ⬅️" 노출(forks 순서와 일치) → mapRevealS
  경과 후 자동으로 걷기 안내문 전환 → 기억대로 조향 시 2/2 정답·⭐⭐⭐.
- **전체 하루 15과제 실사(實使) 흐름** 내에서 3개 걷기 구간 모두 재확인 — 실제 데모 세션
  시퀀스 안에서(연습 메뉴 아닌) 동일하게 정상 동작, 완료 화면 "치료사용 기록"에 "게이트 2/2"·
  "갈림길 2/2" 수치 정확히 렌더링. 콘솔 에러 0.

**다음(미착수)**: P5 이중과제·계산·신체 배치 차원(정중선 교차 등). ⚠️ 공용 PC 아동 분리(프로필)
는 여전히 치료사 결정 대기.

## M6.10 — 인지 잔여 차원 (P5) (2026-07-25 구현·검증)
difficulty-progression_v0.1.md §D P5 "이중과제·계수·계산 레이어, 존 링 숨김" 실장. 존 링 숨김은
P2(M6.7)에서 이미 구현됨 — 이번엔 나머지 3개(계산·정중선 교차·이중과제+지연회상)에 집중.

**계산/수량(§A-인지 L4)**: 기존에 만들어져 있었지만 데모에서 한 번도 쓰이지 않던
`budget`·`price`·`requiredCount`(StationPhase에 이미 완전히 구현·검증된 기능)를 재사용 —
새 판정 엔진을 만들지 않고 "문구점 들르기"를 재저작했다. 연필(300원)·공책(700원)·지우개(600원)
중 예산 1000원 안에서 2개만 담을 수 있다(전부 담으면 1600원, 초과). distractor로 만들지
않은 것이 핵심 결정 — distractor는 항상 튕겨나가 예산 판단 자체를 우회하므로, 세 개 다
"담을 수도 있는" 정상 후보로 두어야 예산 계산이라는 인지 과제가 성립한다.

**정중선 교차(§A-옮기기 L3)**: 저작 시 별도 플래그를 추가하지 않고, 실제 사물·존 좌표에서
`crossesMidline()`로 유도 — 화면 중앙(x=0.5)을 사이에 두고 사물과 존이 충분히(각 0.08 이상)
떨어져 반대편에 있으면 인정. 이미 존재하는 4개 스테이션(아침 식사·신발 꺼내 신기·문구점·
교실 도착)이 우연이 아니라 실제로 이 조건을 만족해 "정중선교차" 배지가 뜬다 — 정직한 감사
원칙대로 좌표를 조작해 태그를 끼워 맞추지 않고, 실재하는 설계를 드러내기만 했다.

**이중과제 + 지연 회상(§A-인지 L5)**: makeSegment에 `flags`(z·xOffset·color) 필드 추가.
학교 복도 걷기(L5, 갈림길 2회+미니맵)에 주황 깃발 2개·초록 깃발(방해자극) 1개를 배치 —
조향하면서 색으로 세는 것이 "이중과제"(운동+인지 동시 처리)다. `NavPhase`가 통과 관찰
지표(`flagsSeen`/`flagsTotal`, 주황만 셈)를 기록하지만 **아동에게 정답을 요구하지 않는다**
(다른 관찰 지표처럼 저작 참고용). 정답은 "교실 도착" 다음에 새로 추가한 16번째 항목
"아침 조회 — 오는 길 기억하기"(고르기 스텝, `recall:true`)에서 확인 — 중간에 다른 과제
(교실 도착)를 거친 뒤 떠올리게 해 "지연 회상"을 구현한다. 정답 개수는 세션 저작 시 정적으로
고정(런타임 동적 계산 아님) — 데모 콘텐츠가 고정이라 단순하게 유지.

**의도된 단순화(투명하게 기록)**: 팔 도달의 원거리·상단 대역(어깨 위 도달) 차원은
착수하지 않음 — 기존 데모 사물이 전부 y<0.7(중·하단) 범위에 있어, 정직하게 태그하려면
새 라이브러리 사물(옷걸이·높은 선반 등)과 장면 재저작이 필요한데 이번 범위에 포함하지
않았다. 다음 착수 시점에 정할 사항으로 남김.

**runner-app.js 변경**: `buildNavCourse()`에 깃발 프롭(막대+색 깃발) 렌더링, `NavPhase`에
`flagState`(1회성 통과 관찰), `steerCourse`/게이트·커브 안내문에 깃발 유무를 반영한 조합
안내문(`hasFlags` 분기). `cognitiveTags()`에 `crossesMidline()` 신규 헬퍼 + `이중과제`
(flags 존재)· `지연회상`(steps.some(recall)) 배지. `entrySummary`에 "깃발 관찰 N/M(참고용)"
표기(감점 아님을 라벨에 명시). `REPORT_COLS`에 `flagsSeen`/`flagsTotal` 추가.
library-meta.js·library-mesh.js에 `eraser`(지우개) 복셀 1종 신규.

**검증(마우스 모드, 새 탭)**:
- 문구점 예산: 공책(700)→성공, 지우개(600) 추가 시 1300>1000으로 비처벌 튕김(budgetOvers 1)
  확인 → 연필(300) 추가로 정확히 1000원(경계값)에서 완료 확인.
- 학교 복도 걷기: 미니맵 노출 후 안내문이 "외운 경로대로 조향하면서, 주황 깃발도 세어
  봐요"로 정상 결합 표시, 완주 후 `flagsSeen:2/flagsTotal:2`(초록 미포함)·갈림길 2/2·⭐⭐⭐
  확인.
- 아침 조회 회상 스텝: 실제 dwell UI로 "2개" 카드 선택 → 오선택 0·⭐⭐⭐ 확인(정답이 실제로
  올바르게 저작되었음을 실사용 상호작용으로 검증).
- 정중선교차 배지가 아침 식사·신발 꺼내 신기·문구점·교실 도착 4개 스테이션에 정확히 표시.
- **전체 하루 16과제 흐름** 내에서 위 항목 전부 재확인 — 완료 화면 "치료사용 기록"에
  "깃발 관찰 2/2(참고용)" 정확 렌더링. 콘솔 에러 0.

**difficulty-progression_v0.1.md P1~P5 전부 완료.** 남은 것은 문서의 "⚠️ 결정 필요" 항목
(공용 PC 아동 분리, 레벨 수치 현장 캘리브레이션)뿐 — 전부 치료사 현장 결정 대기 사항이며
코드로 해소할 수 없다.

## M6.11 — 서사 분할 + 횡단보도 승격 (R1·R2) (2026-07-25 구현·검증)
mission-roadmap_v0.1.md R1·R2 실장. 미션 선언("일상의 재현·경험 최우선, 자기효능감 향상") 이후
첫 코드 작업 — content-coherence_v0.1.md(등교 중 마트 모순)와 crosswalk-core_v0.1.md(최중요
컨셉 횡단보도의 '살핀다' 부재)가 진단한 결함을 해소한다.

**R1 — 서사 분할(session.js)**: 하루를 실제 occupation 시점에 맞는 두 세션으로 분리.
- `makeDemoSession()` = **1편 "학교 가는 아침"**(13항목): 기상→세면→아침식사→**학교 가방
  챙기기(신설)**→신발→엘리베이터→게이트→버스→커브→횡단보도→갈림길+깃발→교실 도착(가방에서
  꺼내기)→아침 조회(회상). 마트·문구점·카페·길찾기 앱을 전부 제거 — 등굣길에 장보기·주스
  주문이라는 시간 맥락 모순(ecological-validity_v0.1.md 문제4, 07-24 진단·그동안 미적용)을
  이번에 실제로 해소했다.
- `makeErrandSession()` = **2편 "심부름 다녀오기"**(6항목, 신규): 걷기→횡단보도(2번째 노출 =
  장소 변주를 통한 일반화)→마트 심부름→문구점(예산 계산 P5가 원래 있어야 할 자리)→걷기→
  **사 온 것 정리하기(신설 — 우유를 냉장고에)**. 마트 장면의 exit 대사를 "결제 완료! 이제
  집에 가요"(그 직후 문구점·카페로 가던 기존 모순)에서 "결제 완료! 문구점도 들러야겠다"로
  수정 — 장면 자신의 대사와 실제 흐름이 이제 일치한다. "정리하기" 완료가 곧 "집에 왔다"를
  참으로 만드는 순간.
- `makeKioskDrillSession()`(신규): 카페 키오스크는 사용자 결정("별도처리")에 따라 두 서사
  모두에서 제거하고 독립 훈련으로 분리 — 지시문도 "선생님과 함께"로 재프레이밍(아동의 등교·
  심부름 occupation이 아니라 키오스크 조작 자체가 목적이므로).
- 가방 흐름 신설: 아침에 필통·책을 가방에 **넣고**(신규 스테이션, 기존 station 문법 그대로),
  교실 도착에서 같은 사물을 **꺼낸다**(기존 스테이션 재사용, 넣기→꺼내기 호응 완성).
- 러너 UI: 시작 화면에 "1편/2편/☕ 키오스크 사용법 연습" 3개 버튼 추가(runner.html·
  runner-app.js) — 파일 열기 핸들러와 동일한 패턴으로 세션 교체(`applyLevelOverrides` →
  `updateStartTexts` → `renderSuggestCard`).
- 길찾기 앱은 두 서사 모두에서 제거 유지(ecological-validity 기존 결론 이행). 지도의
  실물화는 content-coherence §B 후속 과제로 남김(착수 안 함 — 투명하게 기록).

**R2 — 횡단보도 승격(runner-app.js CrossingPhase, 확정된 결정 반영)**: 세션 최중요 컨셉을
"멈춘다"만 있던 상태에서 "멈춘다→**살핀다**→건넌다"로 완성.
- `makeCrossing()`에 `reps`(반복 횟수)·`lookBothWays`(좌우 살핌 요구)·`ballRep`(공 시나리오
  등장 회차, 1-base) 필드 추가. 1편 배치: `reps:3, lookBothWays:true, ballRep:3`(사용자
  확정) — 1회=연습, 2회=반복, 3회=적용. 2편은 기본값(reps:1)으로 노출만 다시 — 장소 변주를
  통한 일반화가 목적이라 매번 3단계 전체를 요구하지 않는다.
  **의도된 단순화**: 3단계는 별도 콘텐츠가 아니라 동일 로직의 반복(신호 타이밍도 동일) —
  "반복 없는 반복"은 매 회차 빨간불부터 새로 시작해 멈춘다·살핀다·건넌다를 매번 온전히
  요구하는 것으로 구현했다. 왕복(방향 반전)은 만들지 않음(도로 지오메트리 대칭이라 시각적
  차이가 없어 실익 대비 복잡도가 커 보류).
- **좌→우 살핌(사용자 확정: 순서 강제)**: `lean`(기존 입력, 신규 없음) 값으로 왼쪽(400ms
  유지)→오른쪽(400ms 유지) 순서를 만족해야 출발 허가. 순서가 틀리면(오른쪽부터) 그냥
  진행이 안 될 뿐 — 비처벌, 다시 왼쪽부터 하면 된다. 연석에서 대기하는 동안 `navCam`이
  실제로 lean 방향에 비례해 좌우로 패닝(spatial-experience_v0.1.md "시선의 변화" 원칙의
  첫 적용) — 살피는 시늉이 아니라 카메라가 실제로 도로 양쪽을 비추는 경험.
- **공 시나리오(사용자 확정: 회차별 고정 배치, 무작위 아님)**: 지정된 반복 차수의 도로
  진입 1.2초 후 공(복셀 정육면체, 줄무늬)이 좌→우로 1.5초에 걸쳐 굴러감. 연석 잠금과 동일
  원리로 아바타의 실제 x 위치는 절대 움직이지 않음(항상 감쇠) — 실수 자체가 물리적으로
  불가능. 공 쪽으로 기울이는 시도만 관찰 지표(`ballChaseAttempts`, edge-trigger로 지속 유지는
  중복 계수하지 않음)로 기록, 기다림 유지 시 즉시 음성 칭찬.
- 신규 지표 `lookBothDone`·`ballChaseAttempts`를 `REPORT_COLS`/`entrySummary`에 반영(깃발과
  동일하게 "참고용" 표기로 비처벌 관찰 지표임을 명시). `cognitiveTags`에 `lookBothWays` →
  '시각탐색' 배지, `stageLabel`에 반복 횟수 표기.
- 신호등 픽토그램(사람 모양) 리디자인은 이번 범위에서 착수하지 않음 — visual-perception
  §R3(색·실루엣 교정)와 함께 처리하는 것이 더 일관됨(투명하게 기록, 다음 착수 시점 결정 대기).

**R2 부수 — 음성 안내(TTS, 사용자 확정)**: `speak()`(Web Speech API, `ko-KR`, 온디바이스·
외부 자산 없음) + `setInstruction()` 헬퍼로 HUD 지시문 표시와 발화를 한 곳에서 통합 —
기존 8곳의 `hudInstruction.textContent` 직접 대입을 전부 교체. 동일 문구 반복 시 재발화
방지(`_lastSpoken`). 기존 음소거 버튼(M)이 효과음과 음성을 함께 제어(발화 중 음소거 시
`speechSynthesis.cancel()`). visual-perception_v0.1.md가 진단한 "텍스트 지시만 있고 음성
0줄" 결함— 문해 이전 단계인 발달장애 아동 대상에게 이것 없이는 어떤 지시도 전달되지
않는다는 진단에 대한 응답.

**신규 라이브러리**: `fridge`(냉장고, library-meta.js/library-mesh.js) — 2편의 정리 과제에
필요.

**검증(마우스 모드, 새 탭)**:
- 좌우살핌 순서 강제: 오른쪽부터 시도 시 진행 안 됨(lookState 'none' 유지) 확인 → 왼쪽 유지
  후 오른쪽 유지 시 정확히 'done' 전환·`lookBothDone` 증가 확인.
- 3회 반복 구조: 1/3→2/3→3/3 순차 전환, 매 회차 살핌 재요구, 최종 회차에서만 공 시나리오
  등장 확인.
- 공 시나리오: rolling 상태 진입·1.5초 후 자동 종료, 공 쪽 lean 시 `ballChaseAttempts`
  edge-trigger 정확(지속 유지는 1회로만 계수, 중립 복귀 후 재시도는 신규 계수) 확인.
- 1편 전체 13항목 실제 흐름 재생 — 신규 가방 스테이션·승격된 횡단보도 전부 정상, 완료
  화면에 "좌우살핌 3회" 정확 렌더링. 콘솔 에러 0.
- 2편 전체 6항목·키오스크 독립 세션 각각 완주 확인, 콘솔 에러 0.
- 냉장고 신규 사물 렌더링 스크린샷으로 육안 확인(회색 복셀 문·손잡이 라인, 인식 가능한
  실루엣).

**다음(미착수, 투명 기록)**: R3(시지각 교정 — 색 충돌·표상 통일·신호등 픽토그램),
R4(3단계 구조 일반화·지도 실물화), R5(공간 노드·탐색), R6(성공 귀인·마을 건설).
mission-roadmap_v0.1.md 참조.

## M6.12 — 시지각 직관성 교정 (R3) (2026-07-26 구현·검증)
mission-roadmap_v0.1.md R3 실장. visual-perception_v0.1.md가 진단한 6가지 구조적 위험 중
코드로 즉시 해소 가능한 항목들을 처리 — "3초 안에 인식 가능한가"라는 원 질문에 처음으로
답이 되는 조치들(정식 3초 검증 프로토콜 자체는 현장 실행이 필요해 여전히 미착수).

**색 충돌 해소(실제 노출되는 경우만 — 라이브러리 전체 이론상 충돌이 아니라 세션에서
실제로 같은 장면에 등장하는 조합 위주)**:
- 사과(#e53e3e) = 장바구니(#e53e3e) — 마트 심부름 장면에서 "잡을 것"과 "놓을 곳"이 같은
  색이었다. 장바구니를 회색조(#718096)로 변경.
- 냉장고(#e2e8f0) ≈ 우유(#f7fafc) — M6.11에서 신설한 "사 온 것 정리하기" 과제 자체에서
  이미 발생한 저대비 쌍. 냉장고를 더 진한 회색(#a0aec0)으로.
- 빵(#d69e2e) = 비누(#d69e2e) — 현재 두 장면에 동시 등장하지 않아 비노출 상태였지만
  라이브러리 차원의 동일 헥스라 편집기 저작 시 함정이 될 수 있어 함께 정리(비누→#fbb6ce).
- 최소 크기 규범: 지우개·연필·딸기·사탕(0.035 이하)을 0.04로 상향 — 인식용 시각 크기 하한.

**역할별 시각 계층 — LivingScene 드롭존 신설(감사 중 발견한 예상 밖 결함)**: R3 감사 도중
StationPhase의 zoneRing(목표 지점 표시)이 LivingScenePhase의 carryToZone에는 애초에
구현된 적이 없다는 사실을 발견했다 — 마트 심부름·옷장 정리 등 carryToZone을 쓰는 모든
장면에서 "어디에 놓아야 하는지"가 그동안 화면에 전혀 표시되지 않았다(디자인 선택이 아니라
누락). `_buildZoneRing()`을 신설해 방해자극이 아닌 모든 carryToZone prop에 목표 지점
링(StationPhase와 동일한 틸 색 원형 링)을 표시, 완료 시 숨김. 단순 색 대비 조정보다 이번
R3에서 가장 실질적인 시지각 개선.

**표상 통일(핫바 아이콘)**: 이모지 대신 실제 복셀 메시를 오프스크린 렌더러로 스냅샷해
캐싱·재사용(`renderLibIcon()`, 라이브러리 키당 1회 렌더 — 매 프레임 아님). 핫바의 목표
확인과 장면의 실행이 이제 같은 그림으로 보인다(형태 항상성 부담 제거). GLB 커스텀 자산은
비동기 로드라 스냅샷을 생략하고 이모지로 폴백. SelectPhase 카드는 대상 외(물리 사물이
아닌 추상적 선택지가 다수라 범위 밖 — 투명하게 기록).

**신호등 픽토그램**: 원형 램프 2개를 실물 보행신호등과 동일한 사람 그림(정지=서 있는
사람·보행=걷는 사람)으로 교체. 두 포즈가 하나의 Material을 공유해 기존
`.material.emissiveIntensity` 제어 코드는 무변경으로 계속 동작. 초록 잔여시간 카운트다운
바는 이번 범위에서 보류(투명 기록 — 별도 착수 시점 필요).

**커서 의미 재배정**: 잡기(FIST) 커서색을 빨강(#fc8181)에서 호박색(#f6ad55)으로 — 빨강은
신호등 '멈춤' 전용 의미로 남겨 앱 전체 색 의미 일관성 확보(발달장애 대상 원칙).

**의도된 미착수(투명 기록)**: 상호작용 사물의 "밝은 외곽선·펄스" 애니메이션(문서 원안의
동적 강조)은 이번엔 정적 개선(색 충돌 해소·존 링 신설)으로 대체 — 매 프레임 재질 갱신이
필요한 애니메이션 강조는 StationPhase·LivingScenePhase 두 update() 루프 모두를 건드리는
더 큰 작업이라 후속 과제로 남김. 고위험 사물(칫솔·공책·필통 등)의 개별 실루엣 재설계와
정식 3초 인식 검증(치료사·아동 대상 현장 테스트)도 미착수.

**검증(마우스 모드, 새 탭)**: 아침 식사 핫바에서 빵·주스가 실제 메시 스냅샷으로 렌더링
확인(이모지 아님). 마트 심부름에서 장바구니 존 링이 최초로 시각화됨·사과/장바구니 색
분리 확인. 횡단보도에서 정지/보행 픽토그램이 사람 형태로 렌더링됨을 스크린샷으로 확인
(줌인 시 정지=몸통만, 보행=다리 벌린 포즈 구분 가능). 1편 전체 13항목 재생 — 신규
시각 요소 전부와 기존 P1~P5·R1·R2 기능 회귀 없음 확인, 콘솔 에러 0.

## M6.13 — 콘텐츠 상세화: 버스·엘리베이터·카페 (2026-07-26 구현·검증)
사용자 지시 3건 반영. R3의 SelectPhase 개선(핫바 등)과 별개로, 선택 카드 자체의 **다다열
그리드 배치**가 이번에 처음 필요해져 `buildStep()`에 `step.cols` 지원을 신설했다 —
`cols` 미지정 시 기존 한 줄 정렬과 완전히 동일(회귀 없음), 지정 시 열/행 개수에 맞춰 카드
크기·간격을 자동 축소해 화면 안에 배치한다.

- **버스 노선(1편)**: 추상적 번호(3번/7번/12번) 대신 색+번호 조합으로 — 빨강 1200 ·
  **초록 7737(정답)** · 파랑 730. 어떤 노선이 정답인지는 지정되지 않아 초록 7737을
  "학교 가는 버스"로 임의 지정했다 — 틀렸다면 정정 필요.
- **버스 탑승 2단계 수정(사용자 지적)**: "타는 문을 골라요(앞문/뒷문)"는 실제 탑승 행동과
  무관한 억지 선택이었다 — 실제 탑승 루틴인 **교통카드 태그**로 교체("카드 태그" vs
  "그냥 지나가기"). 결제/취소 패턴(카페 키오스크)과 동일한 기존 문법 재사용.
- **엘리베이터(1편)**: 버튼 3개(1층/2층/지하)에서 실제 패널처럼 **2열 5행(10버튼)**으로
  확장 — B1·1~9층. 정답은 기존과 동일하게 1층. `cols:2` 첫 적용 사례.
  10개 좌표는 열/행 배치 자동 계산 결과이며 버튼 텍스트만 저작했다.
  실제 스크린샷으로 2×5 배치 확인.
- **카페 메뉴(독립 키오스크 훈련)**: 3종에서 **20종**으로 확장(`cols:5`, 5열 4행) —
  딸기주스(정답)·아메리카노·카페라떼 등 19개 오답. 20개 카드가 화면 안에 겹침 없이
  정확히 배치됨을 스크린샷으로 확인.

**검증**: 엘리베이터 2×5 그리드·카페 20종 5×4 그리드 스크린샷 확인(카드 겹침·잘림 없음),
버스 옵션 데이터 정확성 디버그 확인, 콘솔 에러 0. `cols` 미지정 스텝(버스 2번째 스텝
"문 고르기" 등 기존 2~3개 옵션 스텝)이 기존과 동일하게 한 줄로 렌더링되어 회귀 없음 확인.
