# 난이도·성장·지표 감사 및 설계 — Difficulty & Progression v0.1
날짜: 2026-07-25 · 상태: 감사+설계(코드 무변경) · 상위: scene-grammar_v0.1.md, therapeutic-goals_by-task_v0.1.md
발단: 사용자 질문 3개 — ①난이도가 차원(범위·방향·정확도·경로·인지 복잡성)을 반영하는가,
②1→15 진행에서 성장하는가, ③결과가 시각적으로 확인되고 다음 훈련 지표가 되는가.

---

# Part 1 — 정직한 감사 (코드 근거)

## Q1. 난이도가 요구 차원을 반영하는가 → **부분적 (대부분 미설계)**

현재 코드의 난이도 장치 전수: `graspRadius`(0.03~0.15) · `tReleaseMs` · `zoneRadius` ·
`hand`(any/L/R/both) · `distractor` · `dwellMs` · 선택지 수 · 스텝 수 · 걷기 `level`(0/1/2) ·
`length`(24/48/72) · `redS/greenS` · `oscTarget`(8회) · `listRevealS`(6초).

| 요구 차원 | 현재 상태 | 판정 |
|---|---|---|
| **팔: 도달 범위** | 사물 좌표가 암묵적으로 결정할 뿐, 근/원거리·높이 대역(어깨 위)·**정중선 교차(대측 도달)** 를 설계 축으로 갖지 않음 | ✗ |
| **팔: 방향** | 수평/수직/대각 도달 구분 없음 — 모든 배치가 임의 | ✗ |
| **팔: 정확도** | graspRadius·zoneRadius·사물 크기 존재 | **○ (유일하게 실재)** |
| **걷기: 직선거리** | length 3단계 — 있으나 실질은 "스트로크 더 많이" = **반복 횟수의 변장** | △ |
| **걷기: 방향전환** | Level 2 lean이 있으나 코스가 직선이라 **조향할 이유가 없음** — 능력은 있는데 과제가 요구하지 않음 | ✗ |
| **걷기: 지도 반영** | 전무 (길찾기 앱은 삭제됨 — 옳은 삭제였으나 공간 표상 과제 자체가 사라짐) | ✗ |
| **인지: 동시처리(이중과제)** | 없음 (횡단보도의 신호 감시+이동이 유일한 근사) | ✗ |
| **인지: 순차처리** | select 다단계, Scene requires 체인(옷→잠옷), zonePass(스캔→담기) | **○** |
| **인지: 시각 피드백** | 존 링·커서·스캔음 존재. 단 "피드백 줄이기"(회상 유도)라는 난이도 축은 없음 | △ |
| **인지: 수 세기→계산** | 없음 — 예산(유일한 수 개념)은 마트 심부름 교체로 제거됨 | ✗ |
| **인지: 예측** | 깜빡임 신호에 출발 억제 정도. 움직이는 목표·타이밍 예측 없음 | △ |

**결론**: "더 어렵게"의 실질이 대부분 **더 많이(횟수·거리·시간)** 다 — 사용자 지적 그대로.
설계된 차원은 정확도 반경과 순차처리뿐이다.

## Q2. 1→15에서 성장하는가 → **아니오**

- 데모 15항목의 배열 원리는 **하루의 시간 서사**이지 난이도 곡선이 아니다. 실제로 항목 1(침실
  Scene)이 인터랙션 유형이 가장 많고(slide·bimanual·carry·파생), 항목 3·4·11은 동일한
  "carry 2개" 과제의 반복이며, 걷기 3회 중 2회는 완전히 같은 설정이다.
- **적응이 없다**: 수행이 좋든 나쁘든 다음 항목·다음 회차는 같은 것을 요구한다. grading은
  세션 JSON에 정적으로 박혀 있다.
- **회차 기록이 없다**: 러너에 localStorage가 0줄. `stagePlayed`/`stageStars`는 메모리 변수라
  새로고침이면 소멸. 리포트는 다운로드 후 앱과 무관해진다.
- 과제 유형별 **위계(1단계→5단계 ladder)가 정의된 적이 없다** — grading 파라미터는 있는데
  "이 값이면 몇 단계"라는 사다리가 없어 치료사가 조절 방향을 알 수 없다.

## Q3. 결과가 시각적이고 다음 훈련의 지표가 되는가 → **아니오**

- 완료 화면은 **텍스트 행 나열**이지 시각화가 아니다(추세·비교·프로파일 차트 없음).
- 회차 간 비교 불가(저장 자체가 없으므로).
- 지표→다음 난이도로 이어지는 연결이 전무 — "도움 2가 발동했으니 다음엔 반경을 넓혀라" 같은
  권고를 시스템이 만들지 않는다. 치료사가 CSV를 내려받아 수동 해석하는 것이 전부.

---

# Part 2 — 설계

## 원칙 (기존 정본과의 정합)
- **서사와 난이도의 분리**: 하루 흐름(1→15)은 시간 서사로 고정하고, 난이도는 **각 과제의
  레벨 축**으로 조절한다. 흐름 순서를 난이도 순서로 재배열하지 않는다(생태학적 타당성 유지).
- **반복 횟수는 난이도가 아니다**: 횟수·거리는 지구력 축으로만 별도 표기하고, 레벨 사다리의
  단계 구분 기준으로 쓰지 않는다.
- **자동이 아니라 제안**: 적응은 시스템이 제안하고 치료사가 승인한다(임상 도구 원칙, A2 계승).
- **P9 실패 없음** 유지: 레벨이 올라도 실패 종결은 생기지 않는다 — assist가 흡수한다.

## A. 난이도 차원 모델 (과제 유형별 사다리 L1~L5)

### 옮기기(carry) 계열 — 신체 차원
| L | 도달 | 방향 | 정확도 | 인지 부가 |
|---|---|---|---|---|
| 1 | 근거리·동측 | 단일 방향 | 큰 사물·큰 존(0.15) | 존 링 항상 표시 |
| 2 | 중거리 | 수평+수직 혼합 | 기본(0.11) | — |
| 3 | **정중선 교차**(좌사물→우존) | 대각 포함 | 축소(0.08) | 방해자극 1 |
| 4 | 원거리+상단 대역(어깨 위 도달) | 혼합 | 소형 사물 | 수량 지정("2개만") |
| 5 | 교차+상단 조합 | 혼합 | 최소 존(0.06) | 존 링 숨김(위치 회상) + 계수 병행 |

### 걷기(이동) — 경로 차원 (신규 개발 필요)
| L | 경로 | 조향 요구 | 공간 표상 |
|---|---|---|---|
| 1 | 직선 short | 없음 | — |
| 2 | 직선 + **게이트 통과**(정렬 조향) | 필요해짐 | — |
| 3 | **커브 코스**(굴곡을 lean으로 추종) | 지속 조향 | — |
| 4 | **갈림길 1회**(표지판 보고 선택) | 선택 조향 | 표지판 읽기 |
| 5 | 갈림길 2회 + **출발 전 미니맵 암기** | 선택 조향 | 지도 기억(길찾기의 올바른 부활) |

### 인지 계열 (select·Scene 공용)
| L | 순차 | 기억 부하 | 수 개념 | 동시처리 |
|---|---|---|---|---|
| 1 | 1스텝·2택 | 목록 상시 표시 | — | — |
| 2 | 2스텝·3택 | listRevealS 8s | 수량 세기("2개") | — |
| 3 | 3스텝 | listRevealS 5s·항목 3개 | 수량+종류 조합 | — |
| 4 | 분기 스텝 | 노출 3s | **단순 계산**(예산 레이어 재결합: "3천원으로?") | 운반 중 계수 |
| 5 | — | 지연 회상(중간 과제 후) | 잔돈 예측 | 걷기 중 표지 계수(이중과제) |

기존 파라미터와의 매핑: 각 레벨은 **grading 프리셋**(graspRadius·dwellMs·listRevealS·존 표시
여부·배치 좌표 패턴의 조합)으로 정의 — 새 판정 엔진이 아니라 기존 값들의 묶음이 1차 구현.
신규 개발이 필요한 것: 걷기 L2~L5(게이트·커브·갈림길·미니맵), 존 링 숨김, 계수 UI, 지연 회상.

## B. 성장 시스템 (Progression)
1. **회차 기록**: localStorage에 회차별 요약 저장 — `{date, 과제유형별: {level, stars, 핵심지표}}`.
   C2 검토: 파생 수치·별점만, 아동 식별정보 없음, 기기 로컬 한정. "기록 지우기"/"내보내기" 버튼
   제공. ⚠️ 공용 치료실 PC에서 아동 간 기록 분리 방법(프로필 코드?)은 치료사 결정 필요.
2. **적응 제안 규칙**(투명·단순): 같은 과제를 도움 0 + 핵심 지표 충족으로 완료 → 다음 회차
   시작 화면에 "지난번 참 잘했어요 — 오늘은 한 단계 어렵게 해볼까요?" 제안 카드(치료사/아동이
   수락·거절). 도움 2 발동 → 한 단계 쉽게 제안. 자동 변경은 하지 않는다.
3. **레벨 가시화**: 스테이지 카드에 과제 레벨 뱃지(Lv.1~5) + 최고 별.

## C. 결과 시각화 & 다음 훈련 지표
1. **완료 화면 개편**: 텍스트 행 → ①영역 프로파일 바(신체 조작/이동/충동억제/작업기억 —
   과제 지표를 영역별 집계), ②회차 추세 스파크라인(별·핵심 지표, canvas 직접 그리기 — 외부
   라이브러리 없음), ③지난 회차 대비 Δ("잡기 실패 3→1 ↓").
2. **다음 훈련 권고문**(규칙 기반, 치료사용): 예 "횡단보도 대기 중 출발시도 4회 — 충동억제
   레벨 유지 권장", "옮기기 도움 0·실패 0 — L3(정중선 교차) 상향 제안". 임상 판단을 대체하지
   않는 참고 문장임을 명기.
3. CSV/JSON에 level·회차 번호 포함(연구·기록용 유지).

## D. 구현 우선순위 (제안)
| P | 내용 | 해소하는 질문 | 규모 |
|---|---|---|---|
| P1 | 회차 기록(localStorage) + 완료 화면 시각화(프로파일·추세·Δ) | Q3 | 소 |
| P2 | 레벨 사다리 정의를 grading 프리셋으로 실장 + 카드 레벨 뱃지 | Q1(팔·인지 일부) | 중 |
| P3 | 적응 제안 카드(치료사 승인형) | Q2 | 소 |
| P4 | 걷기 코스 개편: 게이트→커브→갈림길→미니맵 | Q1(걷기 전체) | **대** |
| P5 | 이중과제·계수·계산 레이어, 존 링 숨김 | Q1(인지 잔여) | 중 |

P1→P2→P3 순이 가장 효율적: 측정·기록이 먼저 있어야 성장 제안이 근거를 갖는다(P3는 P1 의존).
P4는 별도 마일스톤으로 분리 권장.

## ⚠️ 결정 필요
1. 회차 기록의 아동 분리(프로필 코드 vs 기기당 1아동 가정) — 치료실 운영 방식에 달림.
2. 적응 제안의 승인 주체(치료사만 vs 아동도 선택 가능).
3. 레벨 사다리 수치(반경·시간·거리)는 전부 초기 추정 — 현장 캘리브레이션 필요.
4. P4 걷기 개편의 착수 시점(신규 개발 규모 최대).
