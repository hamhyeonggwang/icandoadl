# Dual Perspective System + Diegetic Mirror — MVP 설계
날짜: 2026-07-24 · 상태: 설계 정본(코드 무변경) · 상위: scene-grammar_v0.1.md, ecological-validity-analysis_v0.1.md

---

## 0. 확정 원칙

시점을 하나로 통일하지 않는다. **활동의 목적이 시점을 결정한다.**

```
Navigation Mode        → 3인칭 (Voxel Avatar)
"내가 공간 안에서 어디에 있는가"      — 공간적 자기 위치 확인

Interaction Mode        → Mirror Self-View (실사)
"내가 지금 무엇을 하고 있는가"        — 신체 행위의 체화(embodiment)
```

이 두 시점은 오류가 아니라 **의도된 이중 시점 체계(Dual Perspective System)**다.
전환 지점 자체가 "이동 활동에서 조작 활동으로 넘어간다"는 신호 역할을 한다.

---

## 1. 세 존재자(Three Entities) — 역할 재정의

| 존재자 | 정체 | 담당 모드 | 대응 (Scene Data Model) |
|---|---|---|---|
| **Voxel World** | 환경 — 방·거리·마을의 각진 공간 | 항상 존재(양쪽 모드 배경) | `envObjects` |
| **Voxel Avatar** | 이동하는 나 — 블록 캐릭터 | Navigation 전용 | Scene 간 이동(걷기·횡단보도) |
| **Webcam Self** | 행동하는 나 — 사용자의 실제 신체 영상 | Interaction 전용 | `actions`의 Occupation 수행자 |

**단순화하지 않는다**: 모든 것을 1인칭(가상 눈)이나 아바타로 통일하는 것은 이 구조의 핵심 자산을
버리는 것이다. Voxel Avatar는 "이동"만 맡고, "행동"은 실사 Webcam Self가 맡는다 — 두 존재자가
서로 다른 정체성으로 공존하는 것 자체가 이 게임의 차별점이다.

---

## 2. Diegetic Mirror — MediaPipe를 세계 "안"으로

### 현재 구조의 한계
```
[현재]  videoWrap(풀스크린 웹캠, DOM) ← glcanvas(투명 배경, 3D 오버레이) 위에 겹침
        → 3D 공간이 없다. 화면 전체가 통째로 "카메라 모드"로 전환된다.
        → MediaPipe가 "세계 위에 얹힌 UI 레이어"로 남는다.
```

### 제안 구조
```
[제안]  Voxel Room(3D 공간) 안에 '거울' 오브젝트가 있다
        → 거울 표면 = 실시간 웹캠 텍스처(diegetic — 세계 안의 사물)
        → 사용자는 "화면이 카메라로 바뀌었다"가 아니라
          "voxel 방 안, 거울 앞에 서서 내 모습을 본다"고 인지한다
```

**설계 수준 정의** (코드 아님, 원리만):
- 거울은 `envObjects`의 하나로, `state: "off" | "live"`를 가진다. 캐릭터가 거울 앞 Scene에
  진입하면 `live`로 전환 — 표면이 실시간 자기 모습을 비춘다.
- 상호작용 판정 영역은 **화면 전체가 아니라 거울의 시각적 경계로 한정**된다. 화면 속 다른
  부분(방의 나머지, 창문, 가구)은 계속 Voxel World로 남는다 — 조작 순간에도 공간감이 유지된다.
- 이것이 Scene Grammar **P8(사물은 제자리에서 출발한다)**의 최종 형태다: 이제 "사용자 자신"조차
  화면 전체를 차지하는 대신 방 안의 제자리(거울 앞)에 있다.

### 왜 중요한가 — Scene 전환의 이유가 생긴다
지금은 Navigation→Interaction 전환이 검은 페이드(컷)로만 존재한다. 거울이 월드 오브젝트가 되면:
```
3인칭으로 세면실 진입 → 거울을 향해 걸어감 → 거울 앞에 도달
→ (컷 없이 또는 짧은 전환으로) 거울 표면이 켜짐 → Interaction 시작
```
"다가감"이라는 공간적 행동 자체가 전환의 이유가 된다 — Scene Grammar **P5(변화가 다음 행동의
이유를 만든다)**를 시점 전환에도 적용하는 것.

---

## 3. 적용 범위 — 모든 조작 장면에 거울이 필요한가 (스코프 결정)

**아니다.** Diegetic Mirror는 **실제로 거울이라는 사물이 있는 장소에서만** 쓴다.

| 장소 | 거울류 오브젝트 존재 | 처리 |
|---|---|---|
| 세면실 | 세면대 거울 | **Diegetic Mirror 적용** — 이번 MVP 대상 |
| 현관 | 전신거울(가정에 흔함) | 확장 후보 |
| 주방 조리대·옷장 앞·마트 진열대·문구점 | 물리적 거울 없음 | **기존 방식 유지**(화면 전체 웹캠 배경) — "암묵적 거울"로 남김 |

거울 없는 장소까지 diegetic 구조로 억지로 확장하지 않는다. 이는 Scene Grammar 원칙과 동일한
절제다 — 필요 없는 확장은 복잡도만 늘린다. 대신 이런 장소는 **v-next에서 다른 반사면**(냉장고
문, 쇼윈도)으로 같은 원리를 자연스럽게 넓힐 수 있다는 것만 문서에 남겨둔다.

---

## 4. Scene Grammar와의 결합

- **S1 침실 MVP**(scene-grammar_v0.1.md, 이미 정의됨): 커튼·이불·옷 갈아입기 — 전부 Navigation/
  Voxel Avatar 성격이라 이 Dual Perspective 원칙과 이미 정합적. **변경 불필요.**
- **이 문서가 제안하는 신규 MVP**: **S2 세면실** — 거울 오브젝트가 유일하게 diegetic mirror의
  가치를 검증할 수 있는 자연스러운 장소(P3: 환경이 행동을 제안한다 — 거울은 "보라"는 affordance
  자체를 가진 사물).

두 MVP는 서로 다른 것을 검증한다: S1은 **Scene 문법**(상태·부분순서·파생 오브젝트)을, S2는
**시점 체계**(diegetic mirror)를 검증한다. 순서상 S1을 먼저 하는 것이 여전히 합리적이다 —
S1은 기존 판정 재사용만으로 되지만, S2는 카메라·텍스처 아키텍처 변경이 필요하기 때문이다.

---

## 5. MVP 정의

### MVP-A (선행, 변경 없음): S1 침실
scene-grammar_v0.1.md §G 그대로. Dual Perspective 관점에서는 "이미 정답"인 장면 — 손댈 것 없음.

### MVP-B (신규, 이 문서의 핵심): S2 세면실 — Diegetic Mirror
```
Scene: 세면실
장소: washstand
환경 오브젝트:
  mirror   { states: [off, live], state: off, affords: [approach-mirror] }
  sink     (기존 sink 빌더 재사용 — 거울 앞 받침대)
  towelRack
조작 오브젝트(거울이 live일 때만 등장):
  toothbrush (home: mirror 근처 컵)
행동:
  approach-mirror()          ⇒ mirror.state=live        // 3인칭→거울 시점 전환 트리거
  brush-teeth(mirror=live)   ⇒ hygiene=teethBrushed       // Recognition: GRASP+OSC (신규 판정, scene-grammar C절)
  wash-face(mirror=live)     ⇒ hygiene=faceWashed          // 선택 행동
출구: hygiene includes teethBrushed
```

### 검증 목표 (S1과 구분되는, 이 MVP만의 질문)
1. 사용자가 "화면이 카메라로 바뀌었다"가 아니라 **"방 안 거울에 내가 비친다"**고 인지하는가?
2. 상호작용 판정 영역이 거울 크기로 제한돼도 조작감(잡기·왕복)에 지장이 없는가?
3. 3인칭 걷기 → 거울 시점 전환이 이질적으로 느껴지지 않는가?

---

## 6. 리스크 / ⚠️ 결정 필요 항목 (설계 단계 — 코드 착수 전 확정할 것)

| # | 항목 | 쟁점 |
|---|---|---|
| 1 | 거울 표면 종횡비 | 웹캠 프레임(16:9)과 거울 형태(세면대 거울은 보통 정사각~가로형)가 다를 때 크롭 vs 레터박스 |
| 2 | 손 좌표 재투영 | 기존은 화면 전체 정규화 좌표(0~1)를 그대로 씀. 거울이 화면 일부만 차지하면 손 위치를
    거울의 로컬 uv 좌표로 재매핑해야 함 — 화면 좌표계 → 3D 거울면 좌표계 변환 방식 결정 필요 |
| 3 | 전환 연출 | 페이드 컷 유지 vs "다가가는" 카메라 무빙(§2) — MVP는 페이드 유지 권장, 무빙은 v-next |
| 4 | 성능 | VideoTexture를 3D 표면에 매핑하는 비용 실측 필요(이론상 풀스크린 DOM video보다 가벼울
    가능성이 높으나 확인 전까지 가정하지 않는다) |
| 5 | 재사용성 | 이 구조를 현관 전신거울 등으로 확장할 때 공용 컴포넌트로 일반화할지 여부 |

이 5개는 실제 구현 착수 시 반드시 먼저 답해야 하는 질문이다 — 지금은 결정하지 않는다.

---

## 7. 최종 아키텍처 요약

```
Voxel World (환경, 항상 3D)
 │
 ├─ Navigation Mode ─────────────────────────
 │    Voxel Avatar가 3인칭으로 이동
 │    → "내가 어디에 있는가"
 │
 └─ 거울 오브젝트에 접근 ─────────────────────
      Interaction Mode 진입
      거울 표면 = Webcam Self (diegetic texture)
      → "내가 무엇을 하고 있는가"

      ※ 거울 없는 조작 장면(주방·옷장·마트 등)은
        기존 풀스크린 미러 방식 유지 — 전체를 diegetic으로
        강제 전환하지 않는다 (§3 스코프 절제)
```

## 8. 다음 단계 제안
1. 이 문서 검토·승인
2. **S1 침실**(Scene 문법 검증) 먼저 구현 — 기존 판정만 재사용, 기술 리스크 없음
3. S1이 검증되면 **S2 세면실**(Diegetic Mirror 검증) 착수 — 이때 §6 리스크 5항목을 먼저 확정
